# DJANGO-REACT-SAMPLE/api/urls.py
from django.urls import path

app_name = 'api'

urlpatterns = [
    # Example: path('health/', views.health_check, name='api_health'),
    # This list will be dynamically populated by api/apps.py
]